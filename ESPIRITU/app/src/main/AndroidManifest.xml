<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.daniellemarie.espiritu">

    <application
        android:allowBackup="true"
        android:icon="@drawable/timmy"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".temp"></activity>
        <activity android:name=".speed"></activity>
        <activity android:name=".area"></activity>
        <activity android:name=".volume"></activity>
    </application>

</manifest>